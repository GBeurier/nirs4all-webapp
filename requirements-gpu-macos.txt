# nirs4all webapp Python dependencies (macOS GPU build with MPS)
# For Apple Silicon (M1/M2/M3/M4) GPU acceleration

# Include all CPU dependencies
-r requirements-cpu.txt

# PyTorch with MPS support (Metal Performance Shaders)
# MPS backend is automatically available since PyTorch 1.12+
torch>=2.1.0

# nirs4all with macOS GPU support
# Install with: pip install nirs4all[macos-gpu]
