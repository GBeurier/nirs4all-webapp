# nirs4all webapp Python dependencies (GPU build)
# Includes GPU-accelerated ML libraries for CUDA support

# Include all CPU dependencies
-r requirements-cpu.txt

# GPU-accelerated nirs4all (includes PyTorch with CUDA)
# Install with: pip install nirs4all[gpu]
# This provides:
# - torch>=2.1.0 for GPU-accelerated deep learning
# - Full NICON neural network support
# - GPU-accelerated preprocessing where applicable

# Note: The nirs4all[gpu] extra includes:
# - torch>=2.1.0 with CUDA support
# - Required CUDA runtime libraries (via pip)
#
# System requirements:
# - NVIDIA GPU with CUDA support
# - NVIDIA drivers installed (GPU driver only, not full CUDA toolkit)
# - Linux or Windows
torch>=2.1.0
