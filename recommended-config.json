{
  "schema_version": "1.0",
  "app_version": "1.0.0",
  "nirs4all": "0.7.1",
  "profiles": {
    "cpu": {
      "label": "CPU Only",
      "description": "Core NIRS analysis with scikit-learn models. No GPU required.",
      "packages": {
        "nirs4all": ">=0.7.1"
      }
    },
    "gpu-cuda-torch": {
      "label": "GPU — CUDA + PyTorch",
      "description": "Deep learning with PyTorch on NVIDIA GPUs.",
      "packages": {
        "nirs4all": ">=0.7.1",
        "torch": ">=2.1.0",
        "keras": ">=3.0.0"
      }
    },
    "gpu-cuda-tf": {
      "label": "GPU — CUDA + TensorFlow",
      "description": "Deep learning with TensorFlow on NVIDIA GPUs.",
      "packages": {
        "nirs4all": ">=0.7.1",
        "tensorflow": ">=2.14.0",
        "keras": ">=3.0.0"
      }
    },
    "gpu-metal": {
      "label": "GPU — Apple Metal",
      "description": "Deep learning with TensorFlow on Apple Silicon.",
      "packages": {
        "nirs4all": ">=0.7.1",
        "tensorflow-macos": ">=2.14.0",
        "tensorflow-metal": ">=1.1.0",
        "keras": ">=3.0.0"
      }
    }
  },
  "optional": {
    "shap": {
      "version": ">=0.44",
      "description": "SHAP explanations for model interpretability",
      "category": "explainability"
    },
    "umap-learn": {
      "version": ">=0.5",
      "description": "UMAP dimensionality reduction for data exploration",
      "category": "dimensionality"
    },
    "autogluon": {
      "version": ">=1.0",
      "description": "AutoML framework for automated model selection",
      "category": "automl"
    },
    "jax": {
      "version": ">=0.4.20",
      "description": "JAX numerical computing for advanced models",
      "category": "deep_learning"
    },
    "matplotlib": {
      "version": ">=3.7.0",
      "description": "Plotting library for visualization",
      "category": "visualization"
    },
    "openpyxl": {
      "version": ">=3.1.0",
      "description": "Excel file export support",
      "category": "export"
    }
  }
}
