**Pipeline Builder**
- Extend the number of generators to match all existings in nirs4all

- Operator for sequential (the eqiuvalent of "[" "]" in nirs4all)
-- pipelines in feature aug. (Sequential nesting)
-- pipelines in sample aug. etc. etc.

- Optuna model cannot finetune training params or set finetune training params (basically finetune is a finetuning + a training)



**Global**
- Add locales (fr, de, en, ci)