/**
 * NodeRegistryContext (Phase 2)
 *
 * This file is intentionally a thin re-export so existing imports of
 * "./contexts/NodeRegistryContext" continue to work while the actual
 * implementation lives in NodeRegistryContext.v2.tsx.
 */

export * from "./NodeRegistryContext.v2";
