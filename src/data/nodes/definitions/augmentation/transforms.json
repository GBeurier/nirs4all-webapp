[
  {
    "id": "augmentation.sample_generator",
    "name": "SampleGenerator",
    "type": "augmentation",
    "classPath": "nirs4all.operators.augmentation.SampleGenerator",
    "description": "Generate synthetic samples",
    "longDescription": "Creates synthetic samples using interpolation or generative methods.",
    "category": "Sample Generation",
    "tags": ["augmentation", "sample", "synthetic", "generation"],
    "source": "nirs4all",
    "parameters": [
      {
        "name": "n_samples",
        "type": "int",
        "default": 100,
        "min": 1,
        "max": 10000,
        "description": "Number of samples to generate"
      },
      {
        "name": "method",
        "type": "select",
        "default": "smote",
        "options": [
          { "value": "smote", "label": "SMOTE" },
          { "value": "interpolation", "label": "Linear Interpolation" },
          { "value": "noise", "label": "Add Noise" }
        ],
        "description": "Generation method"
      }
    ]
  },
  {
    "id": "augmentation.noise",
    "name": "NoiseAugmentation",
    "type": "augmentation",
    "classPath": "nirs4all.operators.augmentation.NoiseAugmentation",
    "description": "Add random noise to spectra",
    "longDescription": "Augments dataset by adding random noise to spectral samples.",
    "category": "Noise",
    "tags": ["augmentation", "noise", "random", "perturbation"],
    "source": "nirs4all",
    "parameters": [
      {
        "name": "noise_level",
        "type": "float",
        "default": 0.01,
        "min": 0.001,
        "max": 0.1,
        "step": 0.001,
        "description": "Standard deviation of Gaussian noise"
      },
      {
        "name": "n_augmented",
        "type": "int",
        "default": 2,
        "min": 1,
        "max": 10,
        "description": "Number of augmented copies per sample"
      }
    ]
  },
  {
    "id": "augmentation.mixup",
    "name": "Mixup",
    "type": "augmentation",
    "classPath": "nirs4all.operators.augmentation.Mixup",
    "description": "Mixup augmentation (blend samples)",
    "longDescription": "Creates new samples by blending pairs of existing samples and their targets.",
    "category": "Mixing",
    "tags": ["augmentation", "mixup", "blend", "interpolation"],
    "source": "nirs4all",
    "parameters": [
      {
        "name": "alpha",
        "type": "float",
        "default": 0.4,
        "min": 0.1,
        "max": 1.0,
        "step": 0.1,
        "description": "Beta distribution parameter"
      },
      {
        "name": "n_augmented",
        "type": "int",
        "default": 2,
        "min": 1,
        "max": 10,
        "description": "Number of augmented samples per original"
      }
    ]
  },
  {
    "id": "augmentation.shift",
    "name": "SpectralShift",
    "type": "augmentation",
    "classPath": "nirs4all.operators.augmentation.SpectralShift",
    "description": "Shift spectra along wavelength axis",
    "longDescription": "Augments by shifting spectra along the wavelength axis.",
    "category": "Spectral",
    "tags": ["augmentation", "shift", "wavelength", "spectral"],
    "source": "nirs4all",
    "parameters": [
      {
        "name": "max_shift",
        "type": "int",
        "default": 3,
        "min": 1,
        "max": 20,
        "description": "Maximum shift in pixels/wavelengths"
      }
    ]
  },
  {
    "id": "augmentation.scale",
    "name": "IntensityScale",
    "type": "augmentation",
    "classPath": "nirs4all.operators.augmentation.IntensityScale",
    "description": "Random intensity scaling",
    "longDescription": "Augments by randomly scaling spectral intensity.",
    "category": "Intensity",
    "tags": ["augmentation", "scale", "intensity", "multiplicative"],
    "source": "nirs4all",
    "parameters": [
      {
        "name": "scale_range",
        "type": "string",
        "default": "(0.9, 1.1)",
        "description": "Range of scale factors as tuple"
      }
    ]
  }
]
