/**
 * Generator Node Definitions Index
 */

import core from './core.json';

import type { NodeDefinition } from '../../types';

const allGeneratorNodes: NodeDefinition[] = [
  ...(core as NodeDefinition[]),
];

export default allGeneratorNodes;

export { core };
