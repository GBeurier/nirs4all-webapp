# nirs4all webapp Python dependencies

# Core backend
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0

# Desktop packaging
pywebview>=5.0.0
pyinstaller>=6.12.0

# GTK/WebKit2 backend for WebGL support on Linux (optional but recommended)
# System deps: libgirepository1.0-dev libcairo2-dev gir1.2-gtk-3.0 gir1.2-webkit2-4.1
pycairo>=1.26.0
PyGObject>=3.48.0,<3.50.0  # 3.50+ requires girepository-2.0 (Ubuntu 24.04+)

# nirs4all core (optional - can run without for UI development)
# pip install -e ../nirs4all

# Additional utilities
python-multipart>=0.0.20
aiofiles>=24.0.0

# Update system
httpx>=0.27.0
pyyaml>=6.0
packaging>=24.0
platformdirs>=4.0.0
