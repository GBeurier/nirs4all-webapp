# nirs4all webapp Python dependencies

# Core backend
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
orjson>=3.10.0
msgpack>=1.0.0

# Desktop packaging (PyInstaller for bundling Python backend)
pyinstaller>=6.12.0

# nirs4all core (optional - can run without for UI development)
# pip install -e ../nirs4all

# Additional utilities
python-multipart>=0.0.20
aiofiles>=24.0.0

# Update system
httpx>=0.27.0
pyyaml>=6.0
packaging>=24.0
platformdirs>=4.0.0
